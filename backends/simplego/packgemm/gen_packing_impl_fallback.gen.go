// Code generated by github.com/ajroetker/go-highway/cmd/hwygen. DO NOT EDIT.

package packgemm

import (
	"github.com/ajroetker/go-highway/hwy"
)

func BasePackRHS_fallback_Float16(src []hwy.Float16, dst []hwy.Float16, srcRowStart int, srcColStart int, srcRowStride int, contractingRows int, numCols int, kernelCols int) {
	dstIdx := 0
	numFullStrips := numCols / kernelCols
	fullStripsCol := numFullStrips * kernelCols
	srcStartRowIdx := srcRowStart * srcRowStride
	useScalar := true
	if hwy.CurrentLevel() != hwy.DispatchScalar {
		numLanes := hwy.NumLanes[hwy.Float16]()
		switch {
		case kernelCols == numLanes:
			useScalar = false
			var v0 hwy.Vec[hwy.Float16]
			for stripColIdx := 0; stripColIdx < fullStripsCol; stripColIdx += kernelCols {
				srcIdx := srcStartRowIdx + srcColStart + stripColIdx
				for range contractingRows {
					v0 = hwy.Load(src[srcIdx:])
					hwy.Store(v0, dst[dstIdx:])
					dstIdx += kernelCols
					srcIdx += srcRowStride
				}
			}
		case kernelCols == numLanes*2:
			useScalar = false
			for stripColIdx := 0; stripColIdx < fullStripsCol; stripColIdx += kernelCols {
				srcIdx := srcStartRowIdx + srcColStart + stripColIdx
				for range contractingRows {
					v0 := hwy.Load(src[srcIdx:])
					v1 := hwy.Load(src[srcIdx+numLanes:])
					hwy.Store(v0, dst[dstIdx:])
					hwy.Store(v1, dst[dstIdx+numLanes:])
					dstIdx += kernelCols
					srcIdx += srcRowStride
				}
			}
		case kernelCols == numLanes*4:
			useScalar = false
			var v0, v1, v2, v3 hwy.Vec[hwy.Float16]
			for stripColIdx := 0; stripColIdx < fullStripsCol; stripColIdx += kernelCols {
				srcIdx := srcStartRowIdx + srcColStart + stripColIdx
				for range contractingRows {
					v0 = hwy.Load(src[srcIdx:])
					v1 = hwy.Load(src[srcIdx+numLanes:])
					v2 = hwy.Load(src[srcIdx+numLanes*2:])
					v3 = hwy.Load(src[srcIdx+numLanes*3:])
					hwy.Store(v0, dst[dstIdx:])
					hwy.Store(v1, dst[dstIdx+numLanes:])
					hwy.Store(v2, dst[dstIdx+numLanes*2:])
					hwy.Store(v3, dst[dstIdx+numLanes*3:])
					dstIdx += kernelCols
					srcIdx += srcRowStride
				}
			}
		}
	}
	if useScalar {
		for stripColIdx := 0; stripColIdx < fullStripsCol; stripColIdx += kernelCols {
			srcIdx := srcStartRowIdx + srcColStart + stripColIdx
			for range contractingRows {
				copy(dst[dstIdx:], src[srcIdx:srcIdx+kernelCols])
				dstIdx += kernelCols
				srcIdx += srcRowStride
			}
		}
	}
	validCols := numCols - fullStripsCol
	if validCols == 0 {
		return
	}
	srcIdx := srcStartRowIdx + srcColStart + fullStripsCol
	for range contractingRows {
		copy(dst[dstIdx:], src[srcIdx:srcIdx+validCols])
		dstIdx += validCols
		for range kernelCols - validCols {
			dst[dstIdx] = hwy.Float32ToFloat16(0)
			dstIdx++
		}
	}
}

func BasePackRHS_fallback_BFloat16(src []hwy.BFloat16, dst []hwy.BFloat16, srcRowStart int, srcColStart int, srcRowStride int, contractingRows int, numCols int, kernelCols int) {
	dstIdx := 0
	numFullStrips := numCols / kernelCols
	fullStripsCol := numFullStrips * kernelCols
	srcStartRowIdx := srcRowStart * srcRowStride
	useScalar := true
	if hwy.CurrentLevel() != hwy.DispatchScalar {
		numLanes := hwy.NumLanes[hwy.BFloat16]()
		switch {
		case kernelCols == numLanes:
			useScalar = false
			var v0 hwy.Vec[hwy.BFloat16]
			for stripColIdx := 0; stripColIdx < fullStripsCol; stripColIdx += kernelCols {
				srcIdx := srcStartRowIdx + srcColStart + stripColIdx
				for range contractingRows {
					v0 = hwy.Load(src[srcIdx:])
					hwy.Store(v0, dst[dstIdx:])
					dstIdx += kernelCols
					srcIdx += srcRowStride
				}
			}
		case kernelCols == numLanes*2:
			useScalar = false
			for stripColIdx := 0; stripColIdx < fullStripsCol; stripColIdx += kernelCols {
				srcIdx := srcStartRowIdx + srcColStart + stripColIdx
				for range contractingRows {
					v0 := hwy.Load(src[srcIdx:])
					v1 := hwy.Load(src[srcIdx+numLanes:])
					hwy.Store(v0, dst[dstIdx:])
					hwy.Store(v1, dst[dstIdx+numLanes:])
					dstIdx += kernelCols
					srcIdx += srcRowStride
				}
			}
		case kernelCols == numLanes*4:
			useScalar = false
			var v0, v1, v2, v3 hwy.Vec[hwy.BFloat16]
			for stripColIdx := 0; stripColIdx < fullStripsCol; stripColIdx += kernelCols {
				srcIdx := srcStartRowIdx + srcColStart + stripColIdx
				for range contractingRows {
					v0 = hwy.Load(src[srcIdx:])
					v1 = hwy.Load(src[srcIdx+numLanes:])
					v2 = hwy.Load(src[srcIdx+numLanes*2:])
					v3 = hwy.Load(src[srcIdx+numLanes*3:])
					hwy.Store(v0, dst[dstIdx:])
					hwy.Store(v1, dst[dstIdx+numLanes:])
					hwy.Store(v2, dst[dstIdx+numLanes*2:])
					hwy.Store(v3, dst[dstIdx+numLanes*3:])
					dstIdx += kernelCols
					srcIdx += srcRowStride
				}
			}
		}
	}
	if useScalar {
		for stripColIdx := 0; stripColIdx < fullStripsCol; stripColIdx += kernelCols {
			srcIdx := srcStartRowIdx + srcColStart + stripColIdx
			for range contractingRows {
				copy(dst[dstIdx:], src[srcIdx:srcIdx+kernelCols])
				dstIdx += kernelCols
				srcIdx += srcRowStride
			}
		}
	}
	validCols := numCols - fullStripsCol
	if validCols == 0 {
		return
	}
	srcIdx := srcStartRowIdx + srcColStart + fullStripsCol
	for range contractingRows {
		copy(dst[dstIdx:], src[srcIdx:srcIdx+validCols])
		dstIdx += validCols
		for range kernelCols - validCols {
			dst[dstIdx] = hwy.Float32ToBFloat16(0)
			dstIdx++
		}
	}
}

func BasePackRHS_fallback(src []float32, dst []float32, srcRowStart int, srcColStart int, srcRowStride int, contractingRows int, numCols int, kernelCols int) {
	dstIdx := 0
	numFullStrips := numCols / kernelCols
	fullStripsCol := numFullStrips * kernelCols
	srcStartRowIdx := srcRowStart * srcRowStride
	useScalar := true
	if hwy.CurrentLevel() != hwy.DispatchScalar {
		numLanes := hwy.NumLanes[float32]()
		switch {
		case kernelCols == numLanes:
			useScalar = false
			var v0 hwy.Vec[float32]
			for stripColIdx := 0; stripColIdx < fullStripsCol; stripColIdx += kernelCols {
				srcIdx := srcStartRowIdx + srcColStart + stripColIdx
				for range contractingRows {
					v0 = hwy.Load(src[srcIdx:])
					hwy.Store(v0, dst[dstIdx:])
					dstIdx += kernelCols
					srcIdx += srcRowStride
				}
			}
		case kernelCols == numLanes*2:
			useScalar = false
			for stripColIdx := 0; stripColIdx < fullStripsCol; stripColIdx += kernelCols {
				srcIdx := srcStartRowIdx + srcColStart + stripColIdx
				for range contractingRows {
					v0 := hwy.Load(src[srcIdx:])
					v1 := hwy.Load(src[srcIdx+numLanes:])
					hwy.Store(v0, dst[dstIdx:])
					hwy.Store(v1, dst[dstIdx+numLanes:])
					dstIdx += kernelCols
					srcIdx += srcRowStride
				}
			}
		case kernelCols == numLanes*4:
			useScalar = false
			var v0, v1, v2, v3 hwy.Vec[float32]
			for stripColIdx := 0; stripColIdx < fullStripsCol; stripColIdx += kernelCols {
				srcIdx := srcStartRowIdx + srcColStart + stripColIdx
				for range contractingRows {
					v0 = hwy.Load(src[srcIdx:])
					v1 = hwy.Load(src[srcIdx+numLanes:])
					v2 = hwy.Load(src[srcIdx+numLanes*2:])
					v3 = hwy.Load(src[srcIdx+numLanes*3:])
					hwy.Store(v0, dst[dstIdx:])
					hwy.Store(v1, dst[dstIdx+numLanes:])
					hwy.Store(v2, dst[dstIdx+numLanes*2:])
					hwy.Store(v3, dst[dstIdx+numLanes*3:])
					dstIdx += kernelCols
					srcIdx += srcRowStride
				}
			}
		}
	}
	if useScalar {
		for stripColIdx := 0; stripColIdx < fullStripsCol; stripColIdx += kernelCols {
			srcIdx := srcStartRowIdx + srcColStart + stripColIdx
			for range contractingRows {
				copy(dst[dstIdx:], src[srcIdx:srcIdx+kernelCols])
				dstIdx += kernelCols
				srcIdx += srcRowStride
			}
		}
	}
	validCols := numCols - fullStripsCol
	if validCols == 0 {
		return
	}
	srcIdx := srcStartRowIdx + srcColStart + fullStripsCol
	for range contractingRows {
		copy(dst[dstIdx:], src[srcIdx:srcIdx+validCols])
		dstIdx += validCols
		for range kernelCols - validCols {
			dst[dstIdx] = 0
			dstIdx++
		}
	}
}

func BasePackRHS_fallback_Float64(src []float64, dst []float64, srcRowStart int, srcColStart int, srcRowStride int, contractingRows int, numCols int, kernelCols int) {
	dstIdx := 0
	numFullStrips := numCols / kernelCols
	fullStripsCol := numFullStrips * kernelCols
	srcStartRowIdx := srcRowStart * srcRowStride
	useScalar := true
	if hwy.CurrentLevel() != hwy.DispatchScalar {
		numLanes := hwy.NumLanes[float64]()
		switch {
		case kernelCols == numLanes:
			useScalar = false
			var v0 hwy.Vec[float64]
			for stripColIdx := 0; stripColIdx < fullStripsCol; stripColIdx += kernelCols {
				srcIdx := srcStartRowIdx + srcColStart + stripColIdx
				for range contractingRows {
					v0 = hwy.Load(src[srcIdx:])
					hwy.Store(v0, dst[dstIdx:])
					dstIdx += kernelCols
					srcIdx += srcRowStride
				}
			}
		case kernelCols == numLanes*2:
			useScalar = false
			for stripColIdx := 0; stripColIdx < fullStripsCol; stripColIdx += kernelCols {
				srcIdx := srcStartRowIdx + srcColStart + stripColIdx
				for range contractingRows {
					v0 := hwy.Load(src[srcIdx:])
					v1 := hwy.Load(src[srcIdx+numLanes:])
					hwy.Store(v0, dst[dstIdx:])
					hwy.Store(v1, dst[dstIdx+numLanes:])
					dstIdx += kernelCols
					srcIdx += srcRowStride
				}
			}
		case kernelCols == numLanes*4:
			useScalar = false
			var v0, v1, v2, v3 hwy.Vec[float64]
			for stripColIdx := 0; stripColIdx < fullStripsCol; stripColIdx += kernelCols {
				srcIdx := srcStartRowIdx + srcColStart + stripColIdx
				for range contractingRows {
					v0 = hwy.Load(src[srcIdx:])
					v1 = hwy.Load(src[srcIdx+numLanes:])
					v2 = hwy.Load(src[srcIdx+numLanes*2:])
					v3 = hwy.Load(src[srcIdx+numLanes*3:])
					hwy.Store(v0, dst[dstIdx:])
					hwy.Store(v1, dst[dstIdx+numLanes:])
					hwy.Store(v2, dst[dstIdx+numLanes*2:])
					hwy.Store(v3, dst[dstIdx+numLanes*3:])
					dstIdx += kernelCols
					srcIdx += srcRowStride
				}
			}
		}
	}
	if useScalar {
		for stripColIdx := 0; stripColIdx < fullStripsCol; stripColIdx += kernelCols {
			srcIdx := srcStartRowIdx + srcColStart + stripColIdx
			for range contractingRows {
				copy(dst[dstIdx:], src[srcIdx:srcIdx+kernelCols])
				dstIdx += kernelCols
				srcIdx += srcRowStride
			}
		}
	}
	validCols := numCols - fullStripsCol
	if validCols == 0 {
		return
	}
	srcIdx := srcStartRowIdx + srcColStart + fullStripsCol
	for range contractingRows {
		copy(dst[dstIdx:], src[srcIdx:srcIdx+validCols])
		dstIdx += validCols
		for range kernelCols - validCols {
			dst[dstIdx] = 0
			dstIdx++
		}
	}
}

func BaseApplyPackedOutput_fallback_Float16(packedOutput []hwy.Float16, output []hwy.Float16, alpha hwy.Float16, beta hwy.Float16, packedOutputRowStride int, rowOffset int, colOffset int, outputRowStride int, height int, width int) {
	alphaVec := hwy.Set[hwy.Float16](alpha)
	betaVec := hwy.Set[hwy.Float16](beta)
	for r := range height {
		packedIdx := r * packedOutputRowStride
		outputIdx := (rowOffset+r)*outputRowStride + colOffset
		c := 0
		if hwy.CurrentLevel() != hwy.DispatchScalar {
			numLanes := hwy.NumLanes[hwy.Float16]()
			for ; c+numLanes <= width; c += numLanes {
				packedVal := hwy.Load(packedOutput[packedIdx:])
				outputVal := hwy.Load(output[outputIdx:])
				newVal := hwy.MulAdd(alphaVec, packedVal, hwy.Mul(betaVec, outputVal))
				hwy.Store(newVal, output[outputIdx:])
				packedIdx += numLanes
				outputIdx += numLanes
			}
		}
		for ; c < width; c++ {
			val := packedOutput[packedIdx]
			output[outputIdx] = hwy.Float32ToFloat16(beta.Float32()*output[outputIdx].Float32() + alpha.Float32()*val.Float32())
			packedIdx++
			outputIdx++
		}
	}
}

func BaseApplyPackedOutput_fallback_BFloat16(packedOutput []hwy.BFloat16, output []hwy.BFloat16, alpha hwy.BFloat16, beta hwy.BFloat16, packedOutputRowStride int, rowOffset int, colOffset int, outputRowStride int, height int, width int) {
	alphaVec := hwy.Set[hwy.BFloat16](alpha)
	betaVec := hwy.Set[hwy.BFloat16](beta)
	for r := range height {
		packedIdx := r * packedOutputRowStride
		outputIdx := (rowOffset+r)*outputRowStride + colOffset
		c := 0
		if hwy.CurrentLevel() != hwy.DispatchScalar {
			numLanes := hwy.NumLanes[hwy.BFloat16]()
			for ; c+numLanes <= width; c += numLanes {
				packedVal := hwy.Load(packedOutput[packedIdx:])
				outputVal := hwy.Load(output[outputIdx:])
				newVal := hwy.MulAdd(alphaVec, packedVal, hwy.Mul(betaVec, outputVal))
				hwy.Store(newVal, output[outputIdx:])
				packedIdx += numLanes
				outputIdx += numLanes
			}
		}
		for ; c < width; c++ {
			val := packedOutput[packedIdx]
			output[outputIdx] = hwy.Float32ToBFloat16(beta.Float32()*output[outputIdx].Float32() + alpha.Float32()*val.Float32())
			packedIdx++
			outputIdx++
		}
	}
}

func BaseApplyPackedOutput_fallback(packedOutput []float32, output []float32, alpha float32, beta float32, packedOutputRowStride int, rowOffset int, colOffset int, outputRowStride int, height int, width int) {
	alphaVec := hwy.Set[float32](alpha)
	betaVec := hwy.Set[float32](beta)
	for r := range height {
		packedIdx := r * packedOutputRowStride
		outputIdx := (rowOffset+r)*outputRowStride + colOffset
		c := 0
		if hwy.CurrentLevel() != hwy.DispatchScalar {
			numLanes := hwy.NumLanes[float32]()
			for ; c+numLanes <= width; c += numLanes {
				packedVal := hwy.Load(packedOutput[packedIdx:])
				outputVal := hwy.Load(output[outputIdx:])
				newVal := hwy.MulAdd(alphaVec, packedVal, hwy.Mul(betaVec, outputVal))
				hwy.Store(newVal, output[outputIdx:])
				packedIdx += numLanes
				outputIdx += numLanes
			}
		}
		for ; c < width; c++ {
			val := packedOutput[packedIdx]
			output[outputIdx] = beta*output[outputIdx] + alpha*val
			packedIdx++
			outputIdx++
		}
	}
}

func BaseApplyPackedOutput_fallback_Float64(packedOutput []float64, output []float64, alpha float64, beta float64, packedOutputRowStride int, rowOffset int, colOffset int, outputRowStride int, height int, width int) {
	alphaVec := hwy.Set[float64](alpha)
	betaVec := hwy.Set[float64](beta)
	for r := range height {
		packedIdx := r * packedOutputRowStride
		outputIdx := (rowOffset+r)*outputRowStride + colOffset
		c := 0
		if hwy.CurrentLevel() != hwy.DispatchScalar {
			numLanes := hwy.NumLanes[float64]()
			for ; c+numLanes <= width; c += numLanes {
				packedVal := hwy.Load(packedOutput[packedIdx:])
				outputVal := hwy.Load(output[outputIdx:])
				newVal := hwy.MulAdd(alphaVec, packedVal, hwy.Mul(betaVec, outputVal))
				hwy.Store(newVal, output[outputIdx:])
				packedIdx += numLanes
				outputIdx += numLanes
			}
		}
		for ; c < width; c++ {
			val := packedOutput[packedIdx]
			output[outputIdx] = beta*output[outputIdx] + alpha*val
			packedIdx++
			outputIdx++
		}
	}
}
